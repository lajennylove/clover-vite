import "@styles/app.css";
import.meta.glob(["../images/**", "../fonts/**"]);

// import ExampleAjax from "./exampleAjax";

/**
 * Application entrypoint
 */
addEventListener("DOMContentLoaded", async () => {
  console.log("Clover Vite is ready! 🚀");

  // (new ExampleAjax).hello();
});
